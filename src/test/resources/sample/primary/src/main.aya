open import dep

def  id {A : Type} (a : A) : A => a

def const {A : Type} (B : Type) (a : A) : B -> A => fn _ => a

def doId {A : Type}
  (a : A)
  (b : A) : A => id a

def badMatch (n : Nat) : Nat
| zro => let c d := d in c n

inductive Nat | zro | suc (n : Nat)

open inductive Bool | true | false

def GoodNat : Nat => Nat::zro
def +-comm : Nat => Nat::zro
def + : Nat => Nat::zro

def good : Nat => const Nat +-comm +

def add Nat Nat : Nat
| Nat::zro, b => b
| Nat::suc a, b => Nat::suc (add a b)

module Foo {
  def bar Nat => 0
}

module AmbiA {
  def ambi => 0
}

module AmbiB {
  def ambi => 1
}

open AmbiA
open AmbiB

// you should see `AmbiA::ambi` and `AmbiB::ambi` instead of just one `ambi` here.
def useAmbi => AmbiA::ambi

// 114514 1919 81
def myId (A : Type) : A -> A => fn a => a
