# Sample

This is a aya project for testing

## Steps

0. Open this folder as aya project by selecting `aya.json` as project file.
   You should see `primary` and `dependency` are loaded as idea module.
1. You may click the `+` button in aya prover project view (just like gradle's one),
   and select 
